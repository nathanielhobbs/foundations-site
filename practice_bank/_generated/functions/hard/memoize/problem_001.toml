slug = "functions/hard/memoize/problem_001"
title = "Memoize (cache) a function"
topic = "functions"
difficulty = "hard"

prompt = """
Write `memoize(f)` that returns a new function which caches results by argument.
Assume `f` takes a single hashable argument.
"""

starter_code = """
def memoize(f):
    # TODO
    pass
"""

tests = """
calls = {"n": 0}
def f(x):
    calls["n"] += 1
    return x*x

g = memoize(f)
assert g(3) == 9
assert g(3) == 9
assert calls["n"] == 1
assert g(4) == 16
assert calls["n"] == 2
"""
