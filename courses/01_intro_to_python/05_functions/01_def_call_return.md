## 1. Defining and calling functions (`def`, `return`)

A function packages logic under a name.

- `def` defines it
- `return` sends a value back to the caller

---

### Try it

<button class="tryit" data-try="try01a">Try it: a simple function</button>
<script type="text/plain" id="try01a">
def add(a, b):
    return a + b

print(add(2, 3))
print(add("py", "thon"))
</script>

---

### Your task (for Check step)
Implement `square(n)` that returns `n * n`.
