## 2. Comparisons (`==`, `!=`, `<`, `<=`, `>`, `>=`)

Comparisons produce booleans:

- `3 < 5` → `True`
- `10 == 10` → `True`
- `"a" != "b"` → `True`

---

### Try it

<button class="tryit" data-try="try02a">Try it: comparisons</button>
<script type="text/plain" id="try02a">
print(3 < 5)
print(10 == 10)
print("a" != "b")
print(2 * 3 >= 7)
</script>

---

### Your task (for Check step)
Implement `is_even(n)` which returns `True` if `n` is evenly divisible by 2.
