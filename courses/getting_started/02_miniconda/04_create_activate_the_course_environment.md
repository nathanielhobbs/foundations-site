## Create + activate the course environment

```sh
conda create -n foundations python=3.12 -y
conda activate foundations
python --version
```
If `conda activate` fails on macOS, close/reopen Terminal and try again.
